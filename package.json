{
  "name": "chess-es6.js",
  "version": "0.1.5",
  "description": "Chess library with support for variations, FEN and PGN",
  "directories": {
    "test": "test",
    "lib": "lib"
  },
  "main": "lib/chess.js",
  "scripts": {
    "test": "mocha",
    "build": "babel src --out-dir lib && cp ./definitions/*.d.ts ./lib/"
  },
  "devDependencies": {
    "assert": "^1.4.1",
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "mocha": "^2.5.3"
  },
  "jspm": {
      "jspmNodeConversion": false,
      "format": "cjs",
      "registry": "npm"
  }
}
